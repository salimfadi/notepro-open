// Remplacez cette boucle :
<div className="grid grid-cols-1 md:grid-cols-3 gap-6">
  {sites.map((site, index) => (
    <FavoriteSiteCard 
      key={index} 
      name={site.name} 
      url={site.url} 
      index={index} // Nouveau : Passage de l'index de la carte
      onDelete={handleDeleteSite} // Nouveau : Passage de la fonction de suppression
    />
  ))}
</div>
